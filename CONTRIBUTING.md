# 贡献指南

## 合并纪律：review 必须被「读到」，不靠记忆

自动 review（pr-agent 用 qwen-max、CodeRabbit）会在每个 PR 上贴评论。但**非阻塞的 review 若没人读就是摆设**。为此 main 分支有两道对所有人生效的强制闸（required status checks），不依赖任何人的自觉：

1. **`pr_agent`（软阻断）**：合并前必须等自动 review 跑完——端点通时评论已贴在 PR 上；端点挂时无评论但 check 照样绿（不卡死 PR）。
2. **`review-ack`（读了才能合）**：合并前必须有人留下 **review 结论**，否则 check 红、无法合并。做法二选一：
   - 在 PR 提交一次 review（Approve / Comment / Request changes），或
   - 评论一条以 `review-ack:` 开头（或含「合并前已读」「合并者 review 结论」）的结论，说清 review 有无**真问题**、哪些是**误报**、哪些**采纳**。

> 为什么这样设计：`pr_agent` 保证 review 被**产出**，`review-ack` 保证 review 被**读到并判断**。两道都不依赖 LLM 端点存活；端点挂时 `pr_agent` 自动放行，但 `review-ack` 仍要求人看过——因为「有没有真问题」终究要人判。

## 真机验证纪律：这台机器是多方共享的

本仓的功能（唤醒、投递、presence）**只有真机能验**——「测试全绿真机是坏的」在这条链上已经连续发生九次以上，所以真机验证是硬要求，不是可选项。但验证本身会踩到别人：同一台机器上通常同时跑着多个 Claude / Codex 会话、多个常驻进程，共享同一个桌面、同一份配置目录、同一个工作区。

### 1. 能不用 GUI 就不用（最重要）

**模拟键盘和鼠标是全局的**：打错窗口就是直接给一个正在干活的 agent 注入指令，而且**失败是静默的**——敲下去不会有人告诉你敲到了谁，只有被打断的那一方看得见。2026-08-20（#889）就发生过：一次 AppleScript 按键没先确认目标窗口，打进了另一个 Claude Code 会话，把它打断了。

绝大多数结论不需要敲键盘。优先级：**CLI / RPC / 读文件 / 直接调被测函数 ＞ 起一次性会话 ＞ GUI 模拟输入**。

必须模拟输入时：**每一次**按键前都确认目标 app 处于 frontmost（不是开头确认一次），并在回报里列出验证前后的残留（新窗口、新线程、被切前台的 app 等）。

### 2. 别碰别人的会话与配置

- 不 kill、不注入、不修改**你没有起的**会话；
- 不改用户级配置（`~/.codex/`、`~/.claude/`、`~/.agentparty/agents/` …）。要验证就用临时的 `CODEX_HOME` / `AGENTPARTY_HOME` / 临时 HOME + 一次性会话；
- 注意有些命令**无视这些环境变量**（曾实测 `party hook install --codex` 写死 `homedir()`）。改这类文件前后都要核对，改到了就逐字还原并在回报里说明。

### 3. 验证环境必须照抄故障场景

不能自选一个更容易通过的环境。多次出现「实现方报真机通过、而他那台恰好具备所需前置状态」——例如身份唯一、已有欠账、hook 已被信任。**验收场景要照抄这个功能存在的理由**：功能是为「同目录多身份」而修的，就必须在同目录多身份下验。

### 4. 一旦误触，立刻如实上报

说明影响了谁、造成了什么后果。这条比「不出错」更重要——静默的误触会让后面所有人基于错误状态排查。

### 5. 本机同时有多个生产实例

同名频道可能同时存在于不同 server 上，误投的消息在另一台同样合法、查得到真实内容、毫无破绽。**任何按频道名做的判定都必须带上 server 维度**；操作前先 `party whoami` 确认连的是哪一台。

## 其它约定

- 开自己的分支 + worktree 提 PR；动代码前先 `git fetch` 查重（多 agent 并行开发）。
- 迁移文件用 `worker/migrations/` 下一个空号（四位），过 `scripts/check-migration-numbering.test.ts`。
- Secret 绝不硬编码进代码/工作流——用 GitHub Secret 引用。
